import { useMemo, useState } from 'react';
import { StyleSheet, View } from 'react-native';
import { Button, Card, C, Chip, Eyebrow, Field, Screen, SectionHeader, Stat, Text, Title } from '@/features/khata/ui';
import { AlertTriangleIcon, BoxIcon, PlusIcon, SparkleIcon, UploadIcon } from '@/features/khata/icons';
import { useKhataStore } from '@/features/khata/store';
import type { Bill, Employee, InventoryItem } from '@/features/khata/types';

const money = (value: number) => `NPR ${Math.round(value).toLocaleString()}`;
const num = (value: string) => Number(value) || 0;

export function BillsPanel({ onNavigate }: { onNavigate: (section: string) => void }) {
  const bills = useKhataStore.use.bills();
  return <Screen><Title subtitle="All recorded purchase bills, including scanned documents.">Bills</Title><View style={styles.statRow}><Stat label="Saved bills" value={String(bills.length)} hint="Purchase records" tone="brick" /><Stat label="Purchase total" value={money(bills.reduce((s, b) => s + b.total, 0))} hint="Current period" tone="gold" /></View><SectionHeader title="Purchase register" detail="Supplier, invoice, date and amount" action={<Button label="New purchase" icon={<PlusIcon size={16} color={C.white} />} onPress={() => onNavigate('purchase-scan')} />} />{bills.map(bill => <Record key={bill.id} title={bill.vendor} detail={`${bill.invoice} · ${bill.date} · ${bill.payment}`} amount={money(bill.total)} tone={bill.status === 'saved' ? 'green' : 'gold'} />)}{bills.length === 0 && <Empty text="No purchase bills yet. Scan or enter your first one." />}</Screen>;
}

export function PurchasePanel({ onNavigate }: { onNavigate: (section: string) => void }) {
  const addBill = useKhataStore.use.addBill();
  const [form, setForm] = useState({ vendor: '', invoice: '', total: '', vat: '', payment: 'Cash' });
  const [status, setStatus] = useState('Upload a bill or start a manual entry.');
  const [review, setReview] = useState(false);
  const update = (key: string, value: string) => setForm(current => ({ ...current, [key]: value }));
  const scanDemo = () => { setReview(true); setForm({ vendor: 'Bhatbhateni Supermarket', invoice: 'PUR-1042', total: '8200', vat: '1066', payment: 'Cash' }); setStatus('AI scan complete. Review the highlighted fields before saving.'); };
  const save = () => { if (!form.vendor || !num(form.total)) return; addBill({ vendor: form.vendor, invoice: form.invoice || 'PUR-DRAFT', date: new Date().toISOString().slice(0, 10), total: num(form.total), vat: num(form.vat), payment: form.payment, status: 'saved' }); setStatus('Purchase saved to your local workspace.'); setReview(false); setForm({ vendor: '', invoice: '', total: '', vat: '', payment: 'Cash' }); onNavigate('bills'); };
  return <Screen><Title subtitle="Enter supplier purchases manually or from uploaded bills.">Purchases</Title><Card style={styles.scanCard}><Chip tone="green" icon={<SparkleIcon size={12} color={C.green} />}>OCR review workflow</Chip><Text style={styles.panelTitle}>Scan a purchase bill, then approve the extracted details.</Text><Text style={styles.panelText}>Original documents stay attached to the review record. AI suggestions are never posted without your approval.</Text><View style={styles.buttonRow}><Button label="Choose bill photo / PDF" icon={<UploadIcon size={16} color={C.white} />} onPress={scanDemo} /><Button label="Enter manually" variant="outline" onPress={() => { setReview(true); setStatus('Manual entry ready.'); }} /></View><Text style={styles.status}>{status}</Text></Card><Card><SectionHeader title="Document details" detail={review ? 'Review and correct before saving' : 'Supplier, invoice, tax and payment'} />{review && <Chip tone="gold" icon={<AlertTriangleIcon size={12} color={C.goldDark} />}>AI confidence: review totals</Chip>}<View style={styles.fieldRow}><Field label="Supplier name" value={form.vendor} onChangeText={value => update('vendor', value)} placeholder="Supplier or vendor" /><Field label="Invoice number" value={form.invoice} onChangeText={value => update('invoice', value)} placeholder="PUR-0001" /></View><View style={styles.fieldRow}><Field label="Subtotal / grand total" value={form.total} onChangeText={value => update('total', value)} keyboardType="numeric" placeholder="0" /><Field label="VAT amount" value={form.vat} onChangeText={value => update('vat', value)} keyboardType="numeric" placeholder="0" /></View><View style={styles.buttonRow}><Button label="Approve & save purchase" onPress={save} disabled={!form.vendor || !num(form.total)} /><Button label="Open bills" variant="ghost" onPress={() => onNavigate('bills')} /></View></Card></Screen>;
}

export function SalesInvoicePanel({ onNavigate }: { onNavigate: (section: string) => void }) {
  const addSale = useKhataStore.use.addSale();
  const inventory = useKhataStore.use.inventory();
  const [form, setForm] = useState({ customer: 'Walk-in customer', total: '', cost: '', payment: 'Cash' });
  const update = (key: string, value: string) => setForm(current => ({ ...current, [key]: value }));
  const save = () => { if (!num(form.total)) return; addSale({ customer: form.customer || 'Walk-in customer', date: new Date().toISOString().slice(0, 10), total: num(form.total), cost: num(form.cost), payment: form.payment, itemCount: inventory.length ? 1 : 0 }); setForm({ customer: 'Walk-in customer', total: '', cost: '', payment: 'Cash' }); onNavigate('sales'); };
  return <Screen><Title subtitle="Enter sales invoices manually or from uploaded files.">Sales invoices</Title><Card style={styles.scanCard}><Chip tone="green" icon={<SparkleIcon size={12} color={C.green} />}>Sales capture</Chip><Text style={styles.panelTitle}>Create a sales invoice with stock and gross profit in view.</Text><Text style={styles.panelText}>Choose a document to review, or enter a simple cash sale in seconds.</Text><View style={styles.buttonRow}><Button label="Choose invoice photo / PDF" icon={<UploadIcon size={16} color={C.white} />} onPress={() => setForm(current => ({ ...current, total: '12500', cost: '7800' }))} /><Chip>{inventory.length} inventory items available</Chip></View></Card><Card><SectionHeader title="Invoice details" detail="Customer, payment and amount" /><View style={styles.fieldRow}><Field label="Customer" value={form.customer} onChangeText={value => update('customer', value)} /><Field label="Payment method" value={form.payment} onChangeText={value => update('payment', value)} placeholder="Cash / Credit / Bank" /></View><View style={styles.fieldRow}><Field label="Invoice total" value={form.total} onChangeText={value => update('total', value)} keyboardType="numeric" placeholder="0" /><Field label="Cost of goods sold" value={form.cost} onChangeText={value => update('cost', value)} keyboardType="numeric" placeholder="0" /></View><View style={styles.buttonRow}><Button label="Save sales invoice" onPress={save} disabled={!num(form.total)} /><Button label="View sales" variant="ghost" onPress={() => onNavigate('sales')} /></View></Card></Screen>;
}

export function SalesPanel({ onNavigate }: { onNavigate: (section: string) => void }) {
  const sales = useKhataStore.use.sales();
  const total = sales.reduce((s, sale) => s + sale.total, 0);
  const profit = sales.reduce((s, sale) => s + sale.total - sale.cost, 0);
  return <Screen><Title subtitle="Record sales, deduct stock, and track gross profit.">Sales</Title><View style={styles.statRow}><Stat label="Revenue" value={money(total)} hint={`${sales.length} invoices`} tone="green" /><Stat label="Gross profit" value={money(profit)} hint={total ? `${((profit / total) * 100).toFixed(1)}% margin` : 'No sales yet'} tone="brick" /></View><SectionHeader title="Sales register" action={<Button label="New invoice" icon={<PlusIcon size={16} color={C.white} />} onPress={() => onNavigate('sales-scan')} />} />{sales.map(sale => <Record key={sale.id} title={sale.customer} detail={`${sale.date} · ${sale.payment} · ${sale.itemCount} items`} amount={money(sale.total)} tone="green" />)}{!sales.length && <Empty text="No sales yet. Create your first invoice." />}</Screen>;
}

export function ExpensesPanel() {
  const expenses = useKhataStore.use.expenses();
  const addExpense = useKhataStore.use.addExpense();
  const [form, setForm] = useState({ category: 'General', description: '', amount: '', payment: 'Cash' });
  const update = (key: string, value: string) => setForm(current => ({ ...current, [key]: value }));
  const save = () => { if (!form.description || !num(form.amount)) return; addExpense({ category: form.category, description: form.description, date: new Date().toISOString().slice(0, 10), amount: num(form.amount), payment: form.payment }); setForm({ category: 'General', description: '', amount: '', payment: 'Cash' }); };
  return <Screen><Title subtitle="Record running business expenses for net profit.">Expenses</Title><View style={styles.statRow}><Stat label="This period" value={money(expenses.reduce((s, e) => s + e.amount, 0))} hint="Operating expenses" tone="red" /><Stat label="Entries" value={String(expenses.length)} hint="Every payment tracked" tone="gold" /></View><Card><SectionHeader title="Add expense" detail="Keep the description clear for reporting" /><View style={styles.fieldRow}><Field label="Category" value={form.category} onChangeText={value => update('category', value)} /><Field label="Description" value={form.description} onChangeText={value => update('description', value)} placeholder="Rent, utilities, transport" /></View><View style={styles.fieldRow}><Field label="Amount" value={form.amount} onChangeText={value => update('amount', value)} keyboardType="numeric" placeholder="0" /><Field label="Payment method" value={form.payment} onChangeText={value => update('payment', value)} /></View><Button label="Save expense" onPress={save} disabled={!form.description || !num(form.amount)} /></Card><SectionHeader title="Expense register" />{expenses.map(expense => <Record key={expense.id} title={expense.description} detail={`${expense.category} · ${expense.date} · ${expense.payment}`} amount={money(expense.amount)} tone="red" />)}</Screen>;
}

export function InventoryPanel() {
  const inventory = useKhataStore.use.inventory();
  const saveInventory = useKhataStore.use.saveInventory();
  const removeInventory = useKhataStore.use.removeInventory();
  const [editing, setEditing] = useState<string | undefined>();
  const [form, setForm] = useState({ name: '', category: 'General', unit: 'pcs', stock: '', dailyRequirement: '', reorderLevel: '5', purchaseCost: '', sellingPrice: '', supplier: '' });
  const summary = useMemo(() => ({ value: inventory.reduce((s, item) => s + item.stock * item.purchaseCost, 0), margin: inventory.reduce((s, item) => s + Math.max(0, item.sellingPrice - item.purchaseCost) * item.stock, 0), low: inventory.filter(item => item.stock <= item.reorderLevel || item.stock <= item.dailyRequirement * 7).length }), [inventory]);
  const update = (key: string, value: string) => setForm(current => ({ ...current, [key]: value }));
  const reset = () => { setEditing(undefined); setForm({ name: '', category: 'General', unit: 'pcs', stock: '', dailyRequirement: '', reorderLevel: '5', purchaseCost: '', sellingPrice: '', supplier: '' }); };
  const edit = (item: InventoryItem) => { setEditing(item.id); setForm({ name: item.name, category: item.category, unit: item.unit, stock: String(item.stock), dailyRequirement: String(item.dailyRequirement), reorderLevel: String(item.reorderLevel), purchaseCost: String(item.purchaseCost), sellingPrice: String(item.sellingPrice), supplier: item.supplier }); };
  const save = () => { if (!form.name) return; saveInventory({ id: editing, name: form.name, category: form.category, unit: form.unit, stock: num(form.stock), dailyRequirement: num(form.dailyRequirement), reorderLevel: num(form.reorderLevel), purchaseCost: num(form.purchaseCost), sellingPrice: num(form.sellingPrice), supplier: form.supplier }); reset(); };
  return <Screen><Title subtitle="Manage products with a clearer view of value, margins, and low stock items.">Inventory</Title><Card style={styles.banner}><Chip tone="green" icon={<BoxIcon size={12} color={C.green} />}>Inventory workspace</Chip><Text style={styles.panelTitle}>Track stock, pricing, and reorder pressure.</Text><Text style={styles.panelText}>{summary.low} item{summary.low === 1 ? '' : 's'} need attention.</Text></Card><View style={styles.statRow}><Stat label="Inventory items" value={String(inventory.length)} hint="Products and stock" tone="brick" /><Stat label="Stock value" value={money(summary.value)} hint="At purchase cost" tone="gold" /><Stat label="Potential margin" value={money(summary.margin)} hint="If all stock sells" tone="green" /></View><Card><SectionHeader title={editing ? 'Edit inventory item' : 'Add inventory item'} detail="Cost, selling price, supplier and reorder levels" action={editing ? <Button label="Reset" variant="ghost" onPress={reset} /> : undefined} /><View style={styles.fieldRow}><Field label="Product name" value={form.name} onChangeText={value => update('name', value)} placeholder="Product name" /><Field label="Category" value={form.category} onChangeText={value => update('category', value)} /><Field label="Unit" value={form.unit} onChangeText={value => update('unit', value)} /></View><View style={styles.fieldRow}><Field label="Current stock" value={form.stock} onChangeText={value => update('stock', value)} keyboardType="numeric" /><Field label="Daily requirement" value={form.dailyRequirement} onChangeText={value => update('dailyRequirement', value)} keyboardType="numeric" /><Field label="Reorder level" value={form.reorderLevel} onChangeText={value => update('reorderLevel', value)} keyboardType="numeric" /></View><View style={styles.fieldRow}><Field label="Purchase cost" value={form.purchaseCost} onChangeText={value => update('purchaseCost', value)} keyboardType="numeric" /><Field label="Selling price" value={form.sellingPrice} onChangeText={value => update('sellingPrice', value)} keyboardType="numeric" /><Field label="Supplier" value={form.supplier} onChangeText={value => update('supplier', value)} /></View><Button label={editing ? 'Update item' : 'Save item'} onPress={save} disabled={!form.name} /></Card><SectionHeader title="Inventory list" detail={`${summary.low} item${summary.low === 1 ? '' : 's'} need attention`} />{inventory.map(item => { const low = item.stock <= item.reorderLevel || item.stock <= item.dailyRequirement * 7; return <Card key={item.id} style={low ? { borderColor: '#DDA98F' } : undefined}><View style={styles.recordTop}><View style={{ flex: 1 }}><Text style={styles.recordTitle}>{item.name}</Text><Eyebrow>{item.category} · {item.unit} · Supplier: {item.supplier || '—'}</Eyebrow><Eyebrow>Stock {item.stock} · Daily need {item.dailyRequirement} · Coverage {item.dailyRequirement ? (item.stock / item.dailyRequirement).toFixed(1) : '—'} days</Eyebrow></View><View style={{ alignItems: 'flex-end', gap: 4 }}><Text style={styles.recordAmount}>{money(item.purchaseCost)}</Text><Eyebrow>Sell {money(item.sellingPrice)}</Eyebrow>{low && <Chip tone="red">Low stock</Chip>}</View></View><View style={styles.buttonRow}><Button label="Edit" variant="outline" onPress={() => edit(item)} /><Button label="Delete" variant="danger" onPress={() => removeInventory(item.id)} /></View></Card>; })}</Screen>;
}

export function EmployeesPanel() {
  const employees = useKhataStore.use.employees();
  const benefits = useKhataStore.use.benefits();
  const saveEmployee = useKhataStore.use.saveEmployee();
  const removeEmployee = useKhataStore.use.removeEmployee();
  const saveBenefit = useKhataStore.use.saveBenefit();
  const [form, setForm] = useState({ name: '', department: 'General', phone: '', salary: '' });
  const [benefitForm, setBenefitForm] = useState({ employeeId: '', type: 'Bonus', amount: '' });
  const save = () => { if (!form.name) return; saveEmployee({ name: form.name, department: form.department, phone: form.phone, salary: num(form.salary), status: 'active' }); setForm({ name: '', department: 'General', phone: '', salary: '' }); };
  return <Screen><Title subtitle="Manage staff, salary, benefits, and payroll expenses.">Employees</Title><View style={styles.statRow}><Stat label="Active staff" value={String(employees.filter(employee => employee.status === 'active').length)} hint="Current team" tone="brick" /><Stat label="Benefits ledger" value={money(benefits.reduce((s, benefit) => s + benefit.amount, 0))} hint="Recorded payments" tone="gold" /></View><Card><SectionHeader title="Add employee" detail="Directory information and salary" /><View style={styles.fieldRow}><Field label="Full name" value={form.name} onChangeText={value => setForm(current => ({ ...current, name: value }))} placeholder="Employee name" /><Field label="Department" value={form.department} onChangeText={value => setForm(current => ({ ...current, department: value }))} /><Field label="Phone" value={form.phone} onChangeText={value => setForm(current => ({ ...current, phone: value }))} keyboardType="phone-pad" /><Field label="Monthly salary" value={form.salary} onChangeText={value => setForm(current => ({ ...current, salary: value }))} keyboardType="numeric" /></View><Button label="Save employee" onPress={save} disabled={!form.name} /></Card><Card><SectionHeader title="Add benefit" detail="Paid benefits are part of the employee expense ledger" /><View style={styles.fieldRow}><Field label="Employee ID" value={benefitForm.employeeId} onChangeText={value => setBenefitForm(current => ({ ...current, employeeId: value }))} placeholder={employees[0]?.id || 'employee id'} /><Field label="Benefit type" value={benefitForm.type} onChangeText={value => setBenefitForm(current => ({ ...current, type: value }))} /><Field label="Amount" value={benefitForm.amount} onChangeText={value => setBenefitForm(current => ({ ...current, amount: value }))} keyboardType="numeric" /></View><Button label="Save benefit" onPress={() => { if (!num(benefitForm.amount)) return; saveBenefit({ employeeId: benefitForm.employeeId || employees[0]?.id || '', type: benefitForm.type, amount: num(benefitForm.amount), date: new Date().toISOString().slice(0, 10), payment: 'Bank transfer' }); setBenefitForm({ employeeId: '', type: 'Bonus', amount: '' }); }} /></Card><SectionHeader title="Team directory" />{employees.map(employee => <Card key={employee.id}><View style={styles.recordTop}><View style={{ flex: 1 }}><Text style={styles.recordTitle}>{employee.name}</Text><Eyebrow>{employee.department} · {employee.phone || 'No phone'} · {employee.status}</Eyebrow></View><Text style={styles.recordAmount}>{money(employee.salary)} / month</Text></View><View style={styles.buttonRow}><Button label="Remove" variant="danger" onPress={() => removeEmployee(employee.id)} /></View></Card>)}</Screen>;
}

function Record({ title, detail, amount, tone }: { title: string; detail: string; amount: string; tone: 'green' | 'gold' | 'red' }) { return <Card><View style={styles.recordTop}><View style={{ flex: 1, gap: 4 }}><Text style={styles.recordTitle}>{title}</Text><Eyebrow>{detail}</Eyebrow></View><View style={{ alignItems: 'flex-end', gap: 5 }}><Text style={[styles.recordAmount, { color: tone === 'green' ? C.greenDark : tone === 'red' ? C.red : '#9C7A2E' }]}>{amount}</Text><Chip tone={tone}>{tone === 'green' ? 'Posted' : tone === 'red' ? 'Expense' : 'Purchase'}</Chip></View></View></Card>; }
function Empty({ text }: { text: string }) { return <Card style={styles.empty}><Text style={styles.panelText}>{text}</Text></Card>; }

const styles = StyleSheet.create({ statRow: { flexDirection: 'row', flexWrap: 'wrap', gap: 12 }, fieldRow: { flexDirection: 'row', flexWrap: 'wrap', gap: 10 }, buttonRow: { flexDirection: 'row', alignItems: 'center', flexWrap: 'wrap', gap: 8 }, scanCard: { backgroundColor: 'rgba(255,255,255,0.92)' }, banner: { backgroundColor: '#FFF8E8', borderColor: '#E4C077' }, panelTitle: { color: C.ink, fontSize: 18, lineHeight: 24, fontWeight: '800' }, panelText: { color: C.muted, fontSize: 13, lineHeight: 20 }, status: { color: C.greenDark, fontSize: 12, fontWeight: '700' }, recordTop: { flexDirection: 'row', alignItems: 'flex-start', gap: 14 }, recordTitle: { color: C.ink, fontSize: 15, fontWeight: '800' }, recordAmount: { color: C.ink, fontSize: 14, fontWeight: '800' }, empty: { alignItems: 'center', paddingVertical: 36 },
});
